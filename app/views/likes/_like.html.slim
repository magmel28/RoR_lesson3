div#like_count = @post.likes.like_post.count
= button_to 'Like', { :controller => :likes, :action => :like, :post_id => @post.id, :user_id => current_user.id,
                     like_post: :true },
            remote: true, id: :like_btn, disabled: user_like_present?